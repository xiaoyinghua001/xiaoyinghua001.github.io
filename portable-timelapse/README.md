# 通用延时视频转换器

这是一个纯浏览器版延时视频工具，不依赖 Mac、Node、Swift 或本地后台服务。

## 使用方式

直接打开 `index.html`，选择视频，设置抽帧间隔和导出参数，然后转换并保存。

## 分享方式

把整个 `portable-timelapse` 文件夹发给别人即可。对方用 Chrome、Edge 或 Safari 新版本打开 `index.html` 就能使用。

## 输出格式

浏览器支持 MP4 时会优先导出 MP4；不支持时会自动导出 WebM。

视频文件只在浏览器本地处理，不会上传到服务器。
